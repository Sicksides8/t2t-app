import type { ImageSourcePropType } from 'react-native';

export const T2TLogo: ImageSourcePropType = require('../../assets/brand/logo-t2t.png');

export const BrainCharacter: ImageSourcePropType = require('../../assets/illustrations/cerebro-t2t.png');

/**
 * Fondo único de la app provisto por diseño. AppBackground lo aplica como
 * `cover` en todas las pantallas. Para revertir, exportar `null`.
 */
export const APP_BACKGROUND_IMAGE: ImageSourcePropType = require('../../assets/fondo.png');
